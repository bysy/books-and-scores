<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Overridden in v14 -->
    <style name="ToolbarStyle" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
    </style>

    <style name="primary_dark_text" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">#DD000000</item>
        <item name="android:textSize">@dimen/TeamNames</item>
    </style>

    <style name="secondary_dark_text" parent="@android:style/TextAppearance.Small">
        <item name="android:textColor">#89000000</item>
        <item name="android:textSize">@dimen/League</item>
    </style>

    <!-- Overridden in v11 -->
    <style name="cardView"/>
</resources>
